export default {
    name: "تست", // الكود الذي يكتبه المستخدم
    description: "فحص عمل البوت",
    async execute(sock, m, args, rights) {
        const from = m.key.remoteJid;

        // تصميم ملكي لرسالة الفحص
        let response = `╭━━━〔 👑  مملكة يونيفرس  〕━━━╮\n`;
        response += `┃        *UNIVERSE KINGDOM STATUS*        ┃\n`;
        response += `╰━━━━━━━━━━━━━━━━━━━━╯\n\n`;
        response += `✨ أهلاً بك يا *${m.pushName}*\n\n`;
        response += `🛡️ *حـقـوق الـتـطـويـر:*\n`;
        response += `${rights}\n\n`;
        response += `_قـم بـإرسـال الأوامـر لـتـجربـتـي_\n\n`;
        response += `╭━━━〔 🛡️ حقوق النشر 〕━━━╮\n`;
        response += `┃   Powered By 👑 مملكة يونيفرس\n`;
        response += `┃   Founder : كيم دوكجا\n`;
        response += `╰━━━━━━━━━━━━━━━━━━━━╯`;

        // إرسال الرسالة، الصورة ستدمج تلقائيًا من نظام الدمج في الاندكس
        await sock.sendMessage(from, { text: response }, { quoted: m });
    }
};